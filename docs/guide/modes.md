# 模式切换

ClaudeCode 提供多种工作模式，每种模式适用于不同的使用场景。理解并正确使用这些模式可以大大提高工作效率。

## 模式类型

### 1. 正常模式 (Normal Mode)

**默认模式**，适合大多数日常开发任务。

#### 特点
- 直接执行任务
- 立即进行代码修改
- 快速响应

#### 适用场景
- 简单的代码修改
- 快速问题修复
- 日常开发任务

#### 示例

```bash
claude
> 帮我添加一个登录功能
[ClaudeCode 直接开始编写代码]
```

### 2. 计划模式 (Plan Mode)

在执行任务前先制定详细计划，等待用户批准后再执行。

#### 特点
- 先规划后执行
- 用户可审查计划
- 降低出错风险

#### 适用场景
- 复杂功能开发
- 大规模重构
- 不确定需求的任务
- 多文件修改

#### 如何使用

ClaudeCode 会在需要时自动进入计划模式，或者你可以明确要求：

```bash
> 请先制定计划：重构用户认证模块
```

#### 计划模式流程

1. **分析需求**：理解任务要求
2. **制定计划**：生成详细的执行计划
3. **用户审查**：展示计划供用户审核
4. **执行**：用户批准后开始执行
5. **反馈**：执行过程中持续反馈进度

#### 示例

```bash
> 重构整个数据库访问层
[ClaudeCode 进入计划模式]
Claude: 我将制定一个计划来重构数据库访问层...

计划：
1. 分析现有数据库访问代码
2. 设计新的 DAO 层结构
3. 创建基础接口和抽象类
4. 逐个迁移现有功能
5. 添加单元测试
6. 更新文档

是否批准此计划？
```

### 3. 对话模式 (Chat Mode)

纯粹的问答交互，不修改代码。

#### 特点
- 只回答问题
- 不修改文件
- 适合学习和咨询

#### 适用场景
- 询问代码解释
- 学习技术概念
- 获取建议和最佳实践
- 代码审查

#### 示例

```bash
> 解释一下这段代码的作用
> 什么是依赖注入？
> 这个架构有什么问题？
```

## 模式切换

### 自动切换

ClaudeCode 会根据任务复杂度自动选择合适的模式：

- **简单任务** → 正常模式
- **复杂任务** → 计划模式
- **纯问答** → 对话模式

### 手动指定

你也可以明确要求使用特定模式：

```bash
# 要求使用计划模式
> 请先制定计划：[任务描述]

# 要求只回答不修改
> 只解释不修改：这段代码做什么？
```

## 模式对比

| 特性 | 正常模式 | 计划模式 | 对话模式 |
|------|---------|---------|---------|
| 执行速度 | ⚡ 快 | 🐢 慢 | ⚡ 快 |
| 安全性 | ⚠️ 中 | ✅ 高 | ✅ 高 |
| 适合任务 | 简单 | 复杂 | 咨询 |
| 修改代码 | ✅ 是 | ✅ 是 | ❌ 否 |
| 需要批准 | ❌ 否 | ✅ 是 | ❌ 否 |

## 最佳实践

::: tip 模式选择建议

1. **日常小改动**：使用正常模式
2. **大型重构**：使用计划模式
3. **学习和咨询**：使用对话模式
4. **不确定时**：让 ClaudeCode 自动选择

:::

::: warning 注意事项

- 在生产环境中进行重大修改时，建议使用计划模式
- 计划模式下仔细审查计划，确保符合预期
- 对话模式下的建议仍需人工判断

:::

## 实战案例

### 案例 1：添加新功能（正常模式）

```bash
> 添加一个导出 CSV 的功能
[ClaudeCode 直接实现]
```

### 案例 2：系统重构（计划模式）

```bash
> 将整个项目从 JavaScript 迁移到 TypeScript
[ClaudeCode 进入计划模式，制定详细计划]
[用户审查计划]
[批准后执行]
```

### 案例 3：代码审查（对话模式）

```bash
> 审查这个 PR 的代码质量
[ClaudeCode 提供审查意见，不修改代码]
```

## 下一步

- [基本命令](/guide/basic-commands) - 学习常用命令
- [Memory 使用](/advanced/memory) - 配置工作偏好
- [SubAgent 详解](/advanced/subagent) - 了解子任务代理
